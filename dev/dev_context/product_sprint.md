---
title: "MVP Implementation Roadmap"
updated: 2025-12-22
version: 3.0
scope: "planning"
priority: critical
---

# MVP Implementation Roadmap — svaib

## Кратко

**Что:** MVP AI-ассистента для встреч. Бот записывает Zoom/Meet/Teams, транскрибирует, извлекает задачи, отправляет в Telegram и Google Sheets.

**Когда:** 5 недель, дедлайн 27 декабря 2025

**Где сейчас:** Неделя 3 — 🔄 В ПРОЦЕССЕ (22.12.2025)

**Что делаем:** AI Pipeline (протокол + досье) — см. ТЗ v3.1 ниже

## Связанные файлы

- architecture.md — архитектурный контракт (что строим)
- infrastructure.md — текущие ресурсы (на чем строим)

## Текущий статус

**Где:** Неделя 3 — 🔄 В ПРОЦЕССЕ
**Дата:** 22.12.2025
**Дедлайн:** 27 декабря 2025

---

### Неделя 2 — ✅ ЗАВЕРШЕНА (18.12.2025)

**Работает end-to-end:** Telegram → Recall.ai → Soniox → Supabase

| Workflow | Статус |
|----------|--------|
| meeting_create_bot | ✅ Telegram → Client + Meeting → Recall.ai |
| recall_webhook_receiver | ✅ Recall.ai → audio_mixed → Soniox |
| soniox_webhook_receiver | ✅ Soniox → transcript → Supabase |

**Решённые проблемы:**
- ✅ Мульти-трек: переключились на `audio_mixed` (Soniox сам диаризует)
- ✅ pipeline_runs как "мост" между Soniox webhook и meeting

---

### Неделя 3 — что делаем сейчас

**Цель:** После транскрипции → протокол в Telegram + обновление досье.

**Детали:** См. секцию "Неделя 3: AI Pipeline (протокол + досье) — ТЗ v3.1"

**Цепочка 5 промптов:**
```
transcript_cleaner → meeting_analyst → meeting_critic → docs_editor → task_extractor
```

**Первые шаги:**
1. Миграции БД (prompts, documents, изменения в transcripts/meetings)
2. Загрузить 5 промптов в Supabase
3. Создать workflow `meeting_process_transcript`

### Поток данных: Telegram → Transcript

```
Telegram (chat_id)
    │
    ▼
┌─ meeting_create_bot ──────────────────────┐
│  1. Найти/создать client по chat_id       │
│  2. Создать meeting (status: scheduled)   │
│  3. Создать бота Recall.ai                │
│  4. Сохранить recall_bot_id в meeting     │
└───────────────────────────────────────────┘
    │
    ▼
Recall.ai (запись встречи)
    │
    ▼ webhook: audio_separate.done
┌─ recall_webhook_receiver ─────────────────┐
│  1. Найти meeting по recall_bot_id        │
│  2. Создать pipeline_run (soniox_job_id)  │
│  3. Обновить meeting.status → recorded    │
│  4. Отправить аудио в Soniox              │
└───────────────────────────────────────────┘
    │
    ▼
Soniox (транскрибация)
    │
    ▼ webhook: completed
┌─ soniox_webhook_receiver ─────────────────┐
│  1. Найти pipeline_run по soniox_job_id   │
│  2. Получить meeting_id                   │
│  3. Создать transcript                    │
│  4. Обновить meeting.status → transcribed │
│  5. Обновить pipeline_run.status          │
└───────────────────────────────────────────┘
```

### Текущие workflows в n8n

| ID | Название | Нод | Статус | Описание |
|----|----------|-----|--------|----------|
| kG4emaP9j50nZoGu | meeting_create_bot | 9 | ✅ | Telegram → Client + Meeting + Recall.ai |
| 4v1G30AX1eHfRQjF | recall_webhook_receiver | 11 | ✅ | Recall.ai → audio_mixed → Soniox |
| 51ZGGJZp5sINBsQy | soniox_webhook_receiver | 10 | ✅ | Soniox → transcript → Supabase |

**1. meeting_create_bot** ✅ ГОТОВ (17.12.2025)
```
Telegram Trigger → Parse Command → Has Valid URL?
    [true]  → Upsert Client (PG) → Create Meeting → Recall.ai → Update Meeting (PG) → Reply Success
    [false] → Reply Error Format
```
- Использует PostgreSQL UPSERT для клиентов (надёжнее Supabase node)
- Сохраняет recall_bot_id в meeting
- Credential: `Supabase Postgres` (hsHQlDm7rm2EsEGu)

**2. recall_webhook_receiver** ✅ ГОТОВ (17.12.2025)
```
Webhook → Parse → Is Bot Done? → Get Bot → Get Audio → Soniox Transcribe → Find Meeting → Create Pipeline Run → Update Meeting → Respond
```
- ✅ Отправляет аудио в Soniox
- ✅ Ищет meeting по recall_bot_id
- ✅ Создаёт pipeline_run с soniox_job_id в metadata
- ✅ Обновляет meeting.status → recorded

**3. soniox_webhook_receiver** ✅ ГОТОВ (17.12.2025)
```
Webhook → Is Completed? → Get Transcript → Parse Transcript → Find Pipeline Run → Save Transcript → Update Meeting → Update Pipeline Run → Respond OK
```
- ✅ Получает и парсит транскрипт
- ✅ Ищет pipeline_run по soniox_job_id
- ✅ Сохраняет в таблицу transcripts
- ✅ Обновляет meeting.status → transcribed
- ✅ Обновляет pipeline_run.status → completed

---

**Дедлайн:** ~27 декабря 2025 (5 недель)

## Обзор

**Цель:** Работающий MVP для 3-5 пилотных клиентов.

**Что будет работать:**
- Бот приходит на Zoom/Meet/Teams и записывает
- Транскрипт с разметкой спикеров
- AI извлекает задачи и решения
- Задачи записываются в Google Sheets клиента
- Протокол отправляется в Telegram
- Презентация со связкой на Sheets (обновляется автоматически)

**Стартовая точка:**
- ✅ Supabase аккаунт (Free tier)
- ✅ n8n Cloud (решение принято)
- ✅ Домен svaib.com + субдомены
- ✅ VPS Timeweb (резерв)
- ✅ OpenAI API
- ✅ Recall.ai — зарегистрирован (app@svaib.com)
- ✅ Soniox — зарегистрирован (svaib.app@gmail.com)
- ⬜ Google OAuth — не настроен

---

## Принципы работы

**AI по умолчанию**
Любая задача "написать код, SQL, workflow" — сначала формулируешь запрос к AI, не пишешь руками.

**Малые итерации**
Не собираем "идеальный pipeline" за один заход. Сначала минимальный сквозной сценарий, потом улучшаем.

---

## Неделя 1: Фундамент — ✅ ЗАВЕРШЕНА

**Что сделано:**
- Google Cloud Project (svaib-app) + OAuth Consent Screen
- Supabase: 7 таблиц + RLS политики
- n8n Cloud (svaib-app.app.n8n.cloud)
- Recall.ai + Soniox зарегистрированы
- **ASR тест пройден — Soniox выбран** (качество хорошее, диаризация через раздельные треки)
- MCP-серверы: n8n-mcp, supabase-mcp, Context7

---

## Неделя 2: Запись и транскрибация — ✅ ЗАВЕРШЕНА (18.12.2025)

**Что сделано:**
- 3 workflow работают end-to-end (Telegram → Recall.ai → Soniox → Supabase)
- Полный pipeline: scheduled → recorded → transcribed
- Диаризация спикеров работает (audio_mixed + Soniox)
- pipeline_runs как "мост" между Soniox webhook и meeting
- Credentials настроены (см. infrastructure.md)

**Ключевые решения:**
- `audio_mixed` вместо `audio_separate` — упростило flow, Soniox сам диаризует
- `pipeline_runs.metadata` для хранения `soniox_job_id`
- `insert` операция вместо `executeQuery` для INSERT (баг n8n #16354)
- `JSON.stringify()` для JSONB полей

---

## Неделя 3: AI Pipeline (протокол + досье) — ТЗ v3.1

**Цель:** После транскрипции автоматически: качественный анализ → протокол в Telegram → обновление досье проекта.

> **Изменение приоритетов (22.12.2025):** До Нового года важнее запустить рабочий цикл с протоколом. Google Sheets sync переносится на Неделю 4.

### Ключевые решения

| Вопрос | Решение | Почему |
|--------|---------|--------|
| Задачи | Supabase (не Sheets пока) | Сохраняем в tasks, sync в Sheets — Неделя 4 |
| Документы | Одна таблица `documents` | Гибко, doc_type различает (dossier/meeting_protocol/...) |
| Досье | Supabase (не Google Docs) | Проще для MVP. Клиенту пока хватит Telegram |
| Контекст для анализа | Досье + последние 10 протоколов | Страховка от переполнения контекста модели |
| Промпты | Supabase с версионированием | client_id nullable: NULL = системный, UUID = кастомный |
| Идемпотентность | Атомарный UPDATE на `processing` | Защита от гонок (см. ниже) |
| Предобработка | transcript_cleaner | Сжатие 2-3×, экономия токенов |

### Изменения в схеме БД

**Новые таблицы:** `prompts`, `documents` — см. [data_model.md](data_model.md)

**Изменения в существующих:**
- `transcripts` — добавить `cleaned_content TEXT NULL`
- `meetings` — добавить статус `processing` в CHECK constraint

### Архитектура workflow: `meeting_process_transcript`

**Trigger:** `meeting.status = 'transcribed'`

**Защита от гонок (атомарный UPDATE):**
```sql
UPDATE meetings
SET status = 'processing'
WHERE id = $meeting_id AND status = 'transcribed'
RETURNING *
```
Если RETURNING пустой → уже не `transcribed`, выходим.

---

#### Шаг 0: transcript_cleaner

Очистка сырой транскрипции от мусора, сжатие в 2-3 раза.

- **Модель:** gpt-4o-mini (дешёвая)
- **Вход:** `transcripts.content` (сырой)
- **Выход:** чистый транскрипт
- **Сохранение:** `UPDATE transcripts SET cleaned_content = $cleaned WHERE id = $transcript_id`
- **Fallback:** Если cleaner упал → продолжаем с raw content

---

#### Шаг 1: Загрузка контекста

```
1. SELECT meetings.id, meetings.client_id WHERE meetings.id = $meeting_id
2. SELECT * FROM transcripts WHERE meeting_id = $meeting_id ORDER BY created_at DESC LIMIT 1
   → Использовать cleaned_content, fallback на content
3. SELECT * FROM documents WHERE client_id = $client_id AND doc_type = 'dossier'
4. SELECT * FROM documents
   WHERE client_id = $client_id AND doc_type = 'meeting_protocol'
   ORDER BY created_at DESC LIMIT 10
   → Развернуть в хронологию (old→new) перед отправкой в модель
```

---

#### Шаг 2: Цепочка промптов (5 шагов)

| # | role | Модель | Вход | Выход |
|---|------|--------|------|-------|
| 0 | transcript_cleaner | gpt-4o-mini | Сырой транскрипт | Чистый транскрипт |
| 1 | meeting_analyst | gpt-4o-mini | Чистый транскрипт + досье + история | Сырой анализ |
| 2 | meeting_critic | gpt-4o-mini | Сырой анализ | Чистый протокол (markdown) |
| 3 | docs_editor | gpt-4o | Протокол + текущее досье | ПОЛНЫЙ обновлённый текст досье |
| 4 | task_extractor | gpt-4o-mini | Протокол | JSON массив задач |

**Важно для промптов:**
- `meeting_analyst`: "Полнота важнее краткости"
- `meeting_critic`: Жёсткие лимиты (решения ≤5, задачи ≤5, инсайты ≤5)
- `docs_editor`: Фиксированная структура досье, "выход — ПОЛНЫЙ файл, не diff"
- `task_extractor`: JSON mode, "если дедлайн не назван явно — null, не выдумывай"

---

#### Шаг 3: Сохранение

```
1. INSERT pipeline_runs (...) RETURNING id  ← в начале workflow
2. UPSERT documents (досье): client_id + doc_type = 'dossier'
3. INSERT documents (протокол): doc_type = 'meeting_protocol', meeting_id = $meeting_id
   → UNIQUE (meeting_id, doc_type) гарантирует один протокол на встречу
4. INSERT tasks (с генерацией svaib_task_id = hash(meeting_id + normalized(title) + assignee))
5. UPDATE pipeline_runs: status = 'completed', metadata = {...}
6. UPDATE meetings SET status = 'processed'
```

---

#### Шаг 4: Отправка в Telegram

**Лимит Telegram:** 4096 символов.

**Решение для MVP:**
- Если протокол ≤ 4096 → отправляем как есть
- Если > 4096 → split по разделам (если нет заголовков — по 3500-3800 символов)

---

### Логирование (pipeline_runs.metadata)

```json
{
  "steps": [
    {"prompt_role": "transcript_cleaner", "prompt_version": 1, "model": "gpt-4o-mini"},
    {"prompt_role": "meeting_analyst", "prompt_version": 1, "model": "gpt-4o-mini"},
    {"prompt_role": "meeting_critic", "prompt_version": 1, "model": "gpt-4o-mini"},
    {"prompt_role": "docs_editor", "prompt_version": 1, "model": "gpt-4o"},
    {"prompt_role": "task_extractor", "prompt_version": 1, "model": "gpt-4o-mini"}
  ],
  "transcript_raw_length": 12345,
  "transcript_clean_length": 4567,
  "transcript_compression_ratio": 2.7,
  "transcript_used": "clean",
  "tasks_count": 3,
  "protocol_length": 2847
}
```

---

### Telegram Bot

- [x] @BotFather → создать бота (@svaib_bot, 15.12.2025)
- [x] Получить token, добавить в n8n credentials (Telegram svaib)

---

### Чеклист результатов Недели 3

**Схема БД:**
- [ ] Миграция: таблица `prompts`
- [ ] Миграция: таблица `documents`
- [ ] Миграция: `transcripts.cleaned_content`
- [ ] Миграция: `meetings.status` += 'processing'

**Промпты:**
- [ ] 5 промптов загружены в Supabase (transcript_cleaner, meeting_analyst, meeting_critic, docs_editor, task_extractor)

**Workflow:**
- [ ] `meeting_process_transcript` работает
- [ ] Trigger на `status = 'transcribed'`
- [ ] Атомарная защита от гонок
- [ ] Цепочка 5 промптов выполняется
- [ ] Досье обновляется (UPSERT)
- [ ] Протокол сохраняется в documents
- [ ] Задачи сохраняются в tasks
- [ ] Протокол отправляется в Telegram
- [ ] pipeline_runs логирует все шаги

**Тестирование:**
- [ ] Протестировано на 2-3 реальных транскриптах
- [ ] Протокол приходит в Telegram
- [ ] Досье обновляется корректно

---

### Что перенесено

| Было в плане Недели 3 | Куда перенесено |
|-----------------------|-----------------|
| Google OAuth | Неделя 4 |
| Google Sheets sync | Неделя 4 |
| Google Drive (таблица files) | Неделя 4-5 |

---

## Неделя 4: Google Sync + Sheets

**Цель:** Задачи автоматически записываются в Google Sheets клиента.

### Параллельно: OAuth Verification пакет

> **Примечание:** Это не блокер для пилотов. Работаем через test users (до 100 человек).

- [ ] Попросить Claude написать Privacy Policy
- [ ] Попросить Claude написать Terms of Service
- [ ] Задеплоить svaib.com/privacy и svaib.com/terms
- [ ] Записать Loom demo-видео (2-3 мин)
- [ ] Отправить OAuth на verification

### День 1-2: OAuth flow для клиентов

**Проверить:** если verification ещё pending — добавить пилотов как test users.

**n8n workflow: `client_oauth_callback`**
- [ ] Webhook для OAuth callback
- [ ] Exchange code → tokens
- [ ] Сохранить в `oauth_tokens` (encrypted)
- [ ] Создать структуру в Google Drive клиента

### День 3-4: Google Sheets sync

**Создать эталонный Google Sheet:**

| svaib_id (скрытая) | Задача | Ответственный | Дедлайн | Статус | Встреча |
|--------------------|--------|---------------|---------|--------|---------|

**n8n workflow: `sync_tasks_to_sheets`**

- [ ] Trigger: после extract_tasks
- [ ] Читаем tokens клиента из Supabase
- [ ] Проверяем/создаём Sheet "svaib Tasks"
- [ ] **Идемпотентность:**
  - Проверяем `svaib_task_id` в скрытой колонке
  - Если есть — skip, нет — append
- [ ] **Batch write** (не по одной строке!)
- [ ] Логируем в pipeline_runs

### День 5: Error handling + Алерты

- [ ] Добавить try/catch в критичные workflows
- [ ] **Настроить алерты:** если workflow упал → сообщение тебе в Telegram
- [ ] Проверить что pipeline_runs показывает где именно ошибка

### Результат Недели 4 — ПЛАН

- OAuth flow работает
- Задачи пишутся в Google Sheets клиента
- Идемпотентность работает (нет дубликатов)
- Алерты настроены

---

## Неделя 5: RAG + Slides + Пилоты

**Цель:** Полный цикл работает end-to-end. Готовы к пилотам.

> **Примечание:** Telegram Bot уже создан и работает с Недели 3 (базовая отправка протокола).

### День 1-2: RAG lite + Telegram расширение

**RAG lite setup:**
- [ ] Добавить колонку embedding в таблицу documents (Supabase)
- [ ] n8n workflow: `index_client_documents`
  - Читаем файлы из Google Drive клиента (5-10 ключевых)
  - Генерируем summary для каждого файла
  - Создаём embedding (OpenAI text-embedding-3-small)
  - Сохраняем в Supabase
- [ ] n8n workflow: `ai_answer_question`
  - Embed вопроса
  - Semantic search → топ-3 файла
  - Google Docs API → читаем файлы целиком
  - LLM → ответ с контекстом

**Telegram расширение:**
- [ ] Обновить `telegram_send_protocol`: добавить ссылку на Google Sheets
- [ ] Команда /status: статус последней встречи + количество открытых задач

### День 3: Google Slides (нативная связка)

**Виктор делает руками (1-2 часа):**
- [ ] Создать шаблон презентации (4-5 слайдов):
  - Цель проекта
  - Команда
  - Оперативные задачи (связка с Sheets)
  - Ключевые решения
- [ ] Связать таблицу задач: Copy range в Sheets → Paste в Slides → "Link to spreadsheet"
- [ ] Проверить что обновляется при изменении Sheets

**Это не код, не API — нативная фича Google.**

### День 4-5: End-to-end тестирование + Первый пилот

**Полный цикл на себе:**
- [ ] Создать тестового клиента в Supabase
- [ ] Пройти OAuth flow
- [ ] Запустить реальную встречу (30 мин)
- [ ] Проверить весь путь:
  - [ ] Бот пришёл?
  - [ ] Транскрипт появился?
  - [ ] Задачи извлеклись?
  - [ ] Записались в Sheets?
  - [ ] Протокол пришёл в Telegram?
  - [ ] Презентация обновилась?

**Онбординг первого пилота:**
- [ ] Написать инструкцию "Как подключить svaib" (1 страница)
- [ ] Созвон 15 мин — объяснить как работает
- [ ] Помочь пройти OAuth (добавить как test user если нужно)
- [ ] Настроить Telegram чат
- [ ] Запланировать первую встречу с ботом

### Результат Недели 5 — ПЛАН

- Telegram bot работает (протокол + /status)
- Презентация со связкой на Sheets
- Полный цикл протестирован
- Первый пилот онбордился
- **MVP ГОТОВ** 🎉

---

## Что сознательно НЕ входит в MVP

Заложено в архитектуре, но делаем в Phase 1+ (январь):

| Фича | Почему не сейчас |
|------|------------------|
| Обратный sync Sheets → Supabase | Edge cases, лишняя сложность |
| Полный RAG с чанками | RAG lite достаточен для MVP (файлы целиком) |
| ReasoningBank (паттерны команды) | Требует накопленных данных |
| Telegram /ask с AI | Это уже ассистент, не MVP |
| Slides через API | Нативная связка достаточна |
| task_validator (3-й шаг SGR) | 2 шага дают 80% результата |

---

## Технические заметки (на будущее)

### Recall.ai: audio_separate vs audio_mixed (18.12.2025)

**Контекст:** Изначально использовали `audio_separate` (отдельный трек на каждого участника) для лучшей диаризации. Но это создало проблемы с мульти-участниками.

**Решение для MVP:** Переключились на `audio_mixed` (один файл со всеми участниками, Soniox диаризует сам).

**Почему audio_separate требует сложный путь:**

```
API /api/v1/audio_separate/?recording_id=...
    ↓
Возвращает: "download_url": "https://us-west-2.recall.ai/api/v1/download/audio_separate?token=..."
    ↓ (это НЕ прямой URL, а API endpoint!)
Нужен GET на этот URL
    ↓
Получаем массив участников с реальными S3 URL
```

**Почему audio_mixed проще:**

```
Get Bot Details → recordings[0].media_shortcuts.audio_mixed.data.download_url
    ↓
Это УЖЕ прямой S3 URL, никаких дополнительных запросов
```

**Сохранённый flow для audio_separate (n8n backup):**
```
Get Audio URLs → Log Audio Ready → Extract First Track → Fetch Real S3 URL → Extract S3 URL → Soniox
```
Ноды отключены в workflow, можно вернуть если понадобится качественная диаризация через раздельные треки.

**Когда вернуться к audio_separate:**
- Если качество диаризации Soniox на mixed audio окажется недостаточным
- Если появятся клиенты с 5+ участниками где важно точно различать спикеров

---

## Чеклист готовности к production

### Инфраструктура:
- [ ] OAuth работает (verification или test users)
- [ ] RLS настроен и протестирован
- [ ] Backup Supabase включен (автоматический)
- [ ] n8n credentials защищены
- [ ] Алерты при падении workflows

### Интеграции:
- [ ] Recall.ai webhook стабильно работает
- [ ] Soniox точность приемлема для русского
- [ ] Google Sheets batch write работает
- [ ] Telegram bot отвечает

### Качество:
- [ ] pipeline_runs логирует все этапы
- [ ] SGR выдаёт консистентный JSON
- [ ] Идемпотентность задач работает
- [ ] Протестировано на 3+ реальных встречах

---

## Риски и митигации

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| ~~ASR плохо на русском~~ | ✅ Закрыт | Тест пройден, Soniox выбран |
| OAuth verification затянется | Высокая | Работаем с test users (до 100 чел) |
| Recall.ai нестабилен | Низкая | Мониторинг, алерты |
| Промпты дают плохие результаты | Средняя | Итерации на реальных данных |
| LLM выдаёт битый JSON | Средняя | Code Node для "лечения" JSON |

---

## Бюджет MVP (месяц)

| Сервис | Стоимость |
|--------|-----------|
| n8n Cloud Starter | $20 |
| Supabase (Free/Pro) | $0-25 |
| Recall.ai (~10 часов) | $7 |
| Soniox (~10 часов) | $1 |
| OpenAI API | ~$10 |
| **Итого** | **$38-63/мес** |

---

## После MVP: Phase 1 (январь-февраль)

- [ ] Обратный sync Sheets → Supabase
- [ ] Гибридный RAG (chunking для больших документов)
- [ ] Проактивные напоминания
- [ ] task_validator (3-й шаг SGR)
- [ ] Telegram /ask с контекстом

## Phase 2 (март+)

- [ ] ReasoningBank (паттерны команды)
- [ ] Web UI для диалога с ассистентом
- [ ] Domain-Wide Delegation (если >50 клиентов)
- [ ] Ежемесячные дайджесты по паттернам

---

*Документ является основным планом реализации MVP. Отмечай выполненные задачи ✅*
