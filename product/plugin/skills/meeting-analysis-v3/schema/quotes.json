{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Вердикты судьи цитат (ярус 2) — выход узла quote-judge",
  "description": "Быстрая модель судит цитаты, которые не прошли механическую проверку вхождения (ярус 1). exact — есть дословно (код ошибся в нормализации) · paraphrase — смысл в источнике есть, дословности нет: кавычки снимет код · fabricated — смысла в источнике нет: пункт станет спорным.",
  "type": "object",
  "required": ["judge_id", "quotes"],
  "properties": {
    "schema": { "const": "quotes/1" },
    "judge_id": { "type": "string" },
    "quotes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["eid", "quote", "verdict"],
        "properties": {
          "eid": { "type": "string" },
          "quote": { "type": "string", "description": "спорная фраза, как она пришла во флаге" },
          "verdict": { "enum": ["exact", "paraphrase", "fabricated"] },
          "note": { "type": "string", "description": "чем подтверждён или опровергнут смысл" }
        }
      }
    }
  }
}
